一、字段不存在或未赋值时getValue的结果


getValue("xxx")，当xxx不存在或者未被赋值时返回undefined(也为null)

Dot Walking的时候，若未被赋值则为""，字段不存在则为undefined。



二、Current.setWorkflow(false)的使用
```javascript
Current.setWorkflow(false)可以阻止之后的business rule之类的运行
```



三、addQuery中判断时间的注意事项

addQuery中判断时间时，使用filter中事先设定的代码(Today,Yesterday...)j a v a s c r i p t :gs.minutesAgoStart(0)之类的，不会有问题， 其他的gs.nowNoTZ()之类的则会出现问题，该条件无效。



四、遍历map时的注意事项

ServiceNow中使用var a = {}的map，用for(var key in a)遍历时是有序的



五、如何将gr中的Query添加到current中

以下代码可以用来将gr中的Query添加到current中(AND形式，current 的query不消失)、


```javascript
var q = gr.getEncodedQuery();
if (!gs.nil(q))
current.addEncodedQuery(q);
```

六、Reference Qualifier

Reference Qualifer中判断是否为新纪录时不用使用current.isNewRecord()。因为存在临时的sys_id，所以也无法获取到之前的URL。

正确做法是：

```javascript
var test_gr = new GlideRecord(current.getTableName());

gs.addInfoMessage(test_gr.get(current.sys_id));
```
