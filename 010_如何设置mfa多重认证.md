要在Servicenow环境中实现mfa多重认证功能
1.首先在System Applications中找到 Integration - Multifactor Authentication插件进行安装。



2.然后在在System Properties中找到glide.authenticate.multifactor
将它的value设为true，对其进行激活。

3.然后可以在两个地方对其进行设置。

3.1用户列表设置

在用户表中拉出Enable Multifactor Authentication字段，将值改为true

3.2权限设置
在Multi-factor Authentication > Multi-factor Criteria页面中的
Role based multi-factor authentication record中将它的active的值设为true，并添加权限，有此权限的用户在登录时就会被要去mfa验证