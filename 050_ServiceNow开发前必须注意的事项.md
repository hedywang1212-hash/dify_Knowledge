# ServiceNow开发前必须注意的事项
## 1. 新环境开发前必须检查邮件设置。
提交数据前需要检查邮件设置，即使是开发环境，也要保证测试邮件不会误发给客户。如果邮件发送的设定是开启的，那么必须严格设定测试用户。

## 2. 执行删除操作时注意事项。
* 调用API删除记录时需要确保记录不会误删，尤其是使用deleteMultiple()时，会删除所有满足对应GlideRecord检索条件的记录。
> Business Rule中调用current.deleteMultiple()会删除表的所有数据！！

## 3. 创建表和字段前需要严格确认物理名称没有错误，创建完成后不可随意更改。
