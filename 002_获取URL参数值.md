# ServiceNow 获取 URL 参数值

## 概述

在 ServiceNow 中，获取 URL 查询参数的方法取决于代码的运行环境。Client Script（客户端脚本）、服务端脚本和 Service Portal Widget 使用的 API 不同，不能直接混用。

## 核心要点

- 客户端可使用 `parseQuery()` 或浏览器原生的 `URLSearchParams`。
- 服务端可通过 `gs.action.getGlideURI().getMap()` 读取当前请求参数。
- Service Portal Widget 可通过 `$sp.getParameter()` 获取参数。
- 调用前应确认代码运行位置，并处理参数不存在的情况。

## Client Script 中获取参数

### 方法一：解析 `document.URL`

```javascript
function getParameterValue(name) {
  var parameters = document.URL.parseQuery();

  if (parameters[name]) {
    return decodeURI(parameters[name]);
  }

  return undefined;
}

var viewName = getParameterValue('sysparm_view');
```

`parseQuery()` 将 URL 查询字符串解析为对象。如果指定参数不存在，该函数返回 `undefined`。

### 方法二：解析 `location.href`

```javascript
function getParameterValue(name) {
  var parameters = location.href.parseQuery();

  if (parameters[name]) {
    return decodeURI(parameters[name]);
  }

  return undefined;
}

var viewName = getParameterValue('sysparm_view');
```

此方法与解析 `document.URL` 的思路相同，只是 URL 来源改为当前页面的 `location.href`。

### 方法三：使用 `URLSearchParams`

```javascript
var parameters = new URLSearchParams(top.location.href);
var viewName = parameters.get('sysparm_view');
```

`URLSearchParams.get()` 在参数不存在时返回 `null`。使用 `top.location.href` 时，读取的是顶层页面 URL；如果只需要当前页面 URL，可以根据实际页面环境改用 `location.href`。

## 服务端脚本中获取参数

### 读取请求参数

```javascript
var viewName = gs.action
  .getGlideURI()
  .getMap()
  .get('sysparm_view');
```

调用链的作用如下：

- `getGlideURI()`：获取当前请求的 Glide URI。
- `getMap()`：返回包含 URL 参数及其值的 Map。
- `get('sysparm_view')`：读取名为 `sysparm_view` 的参数。

### 判断视图名称

```javascript
var viewName = gs.action
  .getGlideURI()
  .getMap()
  .get('sysparm_view');

if (viewName && viewName.toString() === 'ess') {
  // 当前视图为 ess 时执行的逻辑
}
```

调用 `toString()` 前先确认参数存在，可避免参数缺失时产生空值错误。

## Service Portal Widget 中获取参数

```javascript
var viewName = $sp.getParameter('sysparm_view');
```

`$sp.getParameter()` 适用于 Service Portal Widget 的服务端脚本。传入参数名称后，即可读取当前请求中的对应值。

## 方法选择

- 代码运行在浏览器客户端时，使用 `parseQuery()` 或 `URLSearchParams`。
- 代码运行在传统 ServiceNow 服务端请求上下文中时，使用 `gs.action.getGlideURI()`。
- 代码运行在 Service Portal Widget 服务端时，使用 `$sp.getParameter()`。

## 注意事项

- URL 参数可能不存在。客户端方法可能返回 `undefined` 或 `null`，服务端代码也应先检查返回值。
- 参数值可能经过 URL 编码，读取后应根据实际情况进行解码。
- `sysparm_view` 是 ServiceNow 中常见的视图参数，本例使用 `ess` 作为待判断的视图名称。
