**一、Client Script**
方法一：
```
function getParmVal(name){
var url = document.URL.parseQuery();
if(url[name]){
return decodeURI(url[name]);
}
else{
return undefined;
}
}
```

方法二：
```
function getParmVal(name){
var url = location.href.parseQuery();
if(url[name]){
return decodeURI(url[name]);
}
else{
return undefined;
}
}
```

方法三：
`new URLSearchParams(top.location.href).get(<想要截取的参数>);`


**二、服务端**
```
gs.action.getGlideURI().getMap().get(<想要截取的参数>)
//getGlideURI()允许处理URL参数
//getMap()返回一个map，其中包含了url中的参数和其对应值
```

在服务器端来判断view的名字
`gs.action.getGlideURI().getMap().get('sysparm_view').toString() == <要判断的view的名称>`


**三、widge**
`$sp.getParameter(<想要截取的参数>)`
