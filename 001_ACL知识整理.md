# ServiceNow ACL 访问控制规则

## 概述

ServiceNow ACL（Access Control Rule，访问控制规则）用于控制用户能否访问记录及字段。Record 类型的 ACL 主要分为表级（Row）ACL 和字段级（Field）ACL：同一级别的多条候选规则通常按 OR 关系判定，表级与字段级的结果则按 AND 关系组合。

## 核心要点

- 表级 ACL 决定用户能否访问记录。
- 字段级 ACL 决定用户能否访问记录中的特定字段。
- 同级多条 ACL 中，只要有一条通过即可，即 OR（`||`）关系。
- 访问字段时，表级 ACL 与字段级 ACL 都必须通过，即 AND（`&&`）关系。
- 单条 ACL 内的 `Role`、`Condition` 和 `Script` 必须同时满足。

![ServiceNow ACL 表级与字段级判定关系示意图](https://raw.githubusercontent.com/hedywang1212-hash/dify_Knowledge/main/images/001_01.jpg)

图片说明：图中 `R` 表示表级（Row）ACL，`F` 表示字段级（Field）ACL。同级多条规则以 OR（`||`）关系判定，表级与字段级结果以 AND（`&&`）关系组合。

## ACL 判定逻辑

### 表级与字段级 ACL

ServiceNow 对记录（Record）的访问控制分为两个层级：

1. 先检查表级 ACL，判断用户是否可以访问目标记录。
2. 再检查字段级 ACL，判断用户是否可以访问目标字段。
3. 只有两个层级都通过，用户才可以访问该字段。

同一级别存在多条匹配规则时，只需其中一条通过；表级和字段级之间则必须同时通过。

### ACL 名称与通配符

ACL 的 `Name` 用于指定规则作用的表或字段。`*` 是通配符，通常作为默认规则使用。系统存在更具体的 ACL 时优先使用具体规则；不存在时再使用相应的默认规则。

以 `task` 表为例：

- `task`：`task` 表的表级 ACL。
- `*`：所有表的默认表级 ACL。
- `task.number`：`task` 表中 `number` 字段的字段级 ACL。
- `task.*`：`task` 表所有字段的默认字段级 ACL。

注意：ACL 名称中的 `--None--` 与 `*` 含义不同，配置时不可混用。

### 单条 ACL 的通过条件

一条 ACL 的主要判定项包括：

- `Role`：用户是否具有要求的角色。
- `Condition`：当前记录是否满足配置条件。
- `Script`：自定义脚本是否返回允许访问的结果。

这三个部分是 AND 关系。ACL 中启用的判定项必须全部满足，该条规则才会通过。

## ACL 继承与访问行为

- ACL 可以从父表继承到子表。当子表没有对应规则时，系统会继续使用父表规则。
- 表级 ACL 与字段级 ACL 分别继承。即使子表配置了表级 ACL，用于覆盖父表的表级规则，字段级 ACL 仍可能从父表继承。
- 当 Reference（引用）字段指向父表时，即使用户可以访问某个子表记录，列表或表单仍可能按照父表 ACL 判断引用字段的显示权限。如果用户没有父表访问权限，对应的引用值可能无法显示。

## 管理权限与调试

### Admin overrides

启用 `Admin overrides` 后，管理员会直接通过当前 ACL。它表示当前 ACL 对管理员始终返回通过，并不等同于系统完全忽略这条 ACL。

### security_admin 权限

修改 ACL 需要 `security_admin` 权限。具有相应权限的用户可以通过 `Elevate Roles` 临时提升角色，然后编辑 ACL。只有已经拥有 `security_admin` 权限的用户，才能将该权限授予其他用户。

### Debug Security

可使用 `Debug Security` 调试 ACL。打开记录时，页面会输出参与判定的 ACL 及其结果。启用调试后再使用 `Impersonate User` 模拟其他用户，也可以观察被模拟用户的 ACL 判定结果。

## 其他常用 ACL 类型

除 `Record` 外，常见 ACL 类型还包括：

- `client_callable_script_include`：控制 AJAX 调用是否可以访问相应的 Script Include。
- `REST_Endpoint`：控制 REST Endpoint 的访问权限。
- `ui_page`：控制 UI Page 的访问权限。内部页面使用的 `incident_list.do`、`incident.do` 也属于 UI Page；如果 Record ACL 已通过但仍无法打开表单页面，可进一步检查该类型的 ACL。

## 注意事项

- 如果删除某个表的全部 ACL 配置，该表可能变为不可访问状态。
- 排查权限问题时，应分别检查表级规则、字段级规则以及继承关系。
- Record ACL 通过但页面仍无法打开时，还应检查 `ui_page` 等其他类型的 ACL。
