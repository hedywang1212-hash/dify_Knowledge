```javascript
var gr = new GlideAggregate('表名');

gr.addAggregate('COUNT', '字段名'); //添加需要统计的字段名

gr.addQuery();//添加筛选条件

gr.addQuery(‘字段名’,值);//添加要统计的字段值

gr.query();

if(gr.next()) {

   var count=gr.getAggregate("COUNT", "字段名");//获取在上述筛选条件下该字段值的统计

}
```
