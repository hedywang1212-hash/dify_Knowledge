一、关于"cmn_notif_device"表

1、cmn_notif_device表是什么

  "cmn_notif_device"表——当在sys_user表中创建新用户、更新邮件地址时，会触发Business rule"update email devices"来更新"cmn_notif_device"表。



2、使用XML更改sys_user表时

  单纯通过XML修改"sys_user"表之后，需要更新"cmn_notif_device"表。



3、cmn_notif_device表与sys_user表

  当两表的用户邮箱地址不同时，发送Notifications时会取用cmn_notif_device表中的邮箱地址。



二、关于Inbound Action

1、Inbound Action是什么？

  就是邮件在入站后会触发的动作。



2、Inbound Action的三种类型。

（1）Forward

  转发邮件

（2）Reply

  应答/回复邮件

（3）New

  新邮件



下图是判断在什么情况下运行哪种类型的Inbound Action。



3、如果是reply的Inbound Action，系统会⾃动识别原始邮件关联的record，并且可以用current来取到与邮件相关的Record的值。


Variable
Contents
email.to
包含以逗号分隔的电子邮件列表"收件人/To:”和“抄送/Cc:"框中的地址。
email.direct
在"收件人/To:"框中包含的以逗号分隔的电子邮件地址列表。
email.copied
在"抄送/Cc:"框中包含的以逗号分隔的电子邮件地址列表。
email.body_text
以纯文本字符串的形式包含电子邮件的正文。
email.body_html
以HTML字符串的形式包含电子邮件的正文。
email.from
包含email地址，需要满足以下条件:

如果email Headers字段中列出的地址与现有用户的email地址匹配，则该变量包含该用户的email地址。

如果电子邮件标题字段中列出的地址与现有用户的电子邮件地址不匹配，则此变量包含电子邮件标题字段中列出的地址。

email.from_sys_id
包含向实例发送电子邮件的用户的Sys ID。
email.origemail
包含电子邮件发件人的地址如电子邮件标题字段所列。
email.subject
以纯文本字符串的形式包含电子邮件的主题。
email.recipients
在“收件人/To:”框中以纯文本字符串的形式包含以逗号分隔的收件人地址列表。
email.recipients_array
将收件人地址作为数组包含。
email.content_type
包含电子邮件的MIME内容类型(例如，text/plain; charset="us-ascii" or text/html; charset="us-ascii"）。
email.headers
包含有关发送者、路由、的纯文本字符串发送邮件客户端格式。
email.importance
包含来自发送方的指示关于信息有多重要。的取值为High、Low或空。
4、如果想在Inbound Action中往表中插入新数据
（方法1）使用setWorkflow(false)之后再创建

例：


current.setWorkflow(false);//当前current的表为sn_customerservice_case
var gr = new GlideRecord("sn_customerservice_case");
gr.initialize();
gr.short_description = current.short_description + "新建的Case";
gr.insert(); 
（方法2）将当前current替换


例：


var short_description = current.short_description;
current = new GlideRecord("sn_customerservice_case");
current.initialize();
current.short_description = short_description + "新建的Case";
current.insert();

